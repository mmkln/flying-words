{
  "name": "flying-thoughts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test src/*.test.js",
    "preview": "vite preview",
    "predeploy": "npm test && npm run build",
    "deploy": "gh-pages -d dist --nojekyll -m \"Deploy site\""
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.1.7"
  }
}
